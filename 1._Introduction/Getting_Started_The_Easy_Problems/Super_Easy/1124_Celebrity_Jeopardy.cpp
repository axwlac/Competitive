#include <iostream>
using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    string x;
    while(getline(cin, x)){
        cout << x << "\n";
    }
    return 0;
}